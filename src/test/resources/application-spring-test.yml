spring:
  datasource:
    url: jdbc:hsqldb:mem:testdb
    username: sa
    password: sa
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate.dialect: org.hibernate.dialect.HSQLDialect
